'''import sys imports the sys modules that is built into Python into the current module.
   This module provides access to Python interpreter functions.
    An argument is a value passed to a function it is called.
     sys.argv[] lets us pass in an argument after we run the script.'''
import sys
print("This argument was passed to the script:", sys.argv[1], sys.argv[2], sys.argv[3])
